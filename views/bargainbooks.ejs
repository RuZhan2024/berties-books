<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Bargain Books (&lt; £20)</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/main.css" />
</head>
<body>
  <header class="container">
    <h1>Bargain Books (&lt; £20)</h1>
    <p><a href="/">← Back home</a></p>
  </header>

  <main class="container">
    <% if (!books || books.length === 0) { %>
      <section class="card">
        <p class="muted">No bargain books right now.</p>
      </section>
    <% } else { %>
      <section class="card">
        <ul class="list">
          <% books.forEach(b => { %>
            <li>
              <span class="title"><%= b.name %></span>
              <span class="price">£<%= Number(b.price).toFixed(2) %></span>
            </li>
          <% }) %>
        </ul>
      </section>
    <% } %>
  </main>
</body>
</html>
