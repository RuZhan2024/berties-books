<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Register for <%= shopData.shopName %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="stylesheet" href="/main.css" />
  </head>
  <body>
    <%# Simple registration form (demo). Expects POST handler at /users/registered. %>
    <header class="container">
      <h1>Register for <%= shopData.shopName %></h1>
      <p><a href="/">← Back home</a></p>
    </header>

    <main class="container">
      <%# Relative form action: from /users/register -> POST /users/registered %>
      <form method="POST" action="registered" class="form card">
        <label>First name
          <input id="first" type="text" name="first" required />
        </label>
        <label>Last name
          <input id="last" type="text" name="last" required />
        </label>
        <label>Email
          <input id="email" type="email" name="email" required />
        </label>
        <label>Password
          <input id="password" type="password" name="password" required />
        </label>
        <label>Confirm Password
          <input id="confirm_password" type="password" name="confirm_password" required />
        </label>
        <%if (!!msg) {%>
          <span class="msg"><%=msg %></span>
        <%}%>
        <%# Minimal client-side validation; perform server-side validation and sanitisation. %>
        <button type="submit" class="btn">Register</button>
      </form>
    </main>
  </body>
</html>
