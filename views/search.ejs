<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Search Books</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/main.css" />
</head>
<body>
  <%# Search UI: supports exact and partial name matching. Expects 'locals.keyword', 'locals.mode', and optional 'locals.results'. %>
  <header class="container">
    <h1>Search Books</h1>
    <p><a href="/">← Back home</a></p>
  </header>

  <main class="container">
    <section class="card">
      <%# POST to /books/search; server performs query and re-renders with results. %>
      <form method="post" action="/books/search" class="form">
        <label>Keyword
          <%# Echo previous keyword with nullish coalescing for UX. %>
          <input name="keyword" type="text" value="<%= (locals.keyword ?? '') %>" required />
        </label>

        <fieldset class="inline">
          <legend>Mode</legend>
          <label>
            <%# Partial match (SQL LIKE). Default when mode != 'exact'. %>
            <input type="radio" name="mode" value="partial"
                   <%= (locals.mode !== 'exact') ? 'checked' : '' %> />
            Partial
          </label>
          <label>
            <%# Exact match (SQL =). %>
            <input type="radio" name="mode" value="exact"
                   <%= (locals.mode === 'exact') ? 'checked' : '' %> />
            Exact
          </label>
        </fieldset>

        <button type="submit" class="btn">Search</button>
      </form>
    </section>

    <%# Results section only renders after a search. %>
    <% if (locals.results) { %>
      <section class="card mt-4">
        <h2>Results</h2>
        <% if (locals.results.length === 0) { %>
          <p class="muted">No matching books.</p>
        <% } else { %>
          <table class="table">
            <thead><tr><th>Title</th><th>Price (£)</th></tr></thead>
            <tbody>
            <% locals.results.forEach(r => { %>
              <tr>
                <td><%= r.name %></td>
                <td><%= Number(r.price).toFixed(2) %></td>
              </tr>
            <% }) %>
            </tbody>
          </table>
        <% } %>
      </section>
    <% } %>
  </main>
</body>
</html>
